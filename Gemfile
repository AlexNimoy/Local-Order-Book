# frozen_string_literal: true

source 'https://rubygems.org'

ruby '2.6.5'

gem 'eventmachine'
gem 'faye-websocket'
gem 'rack'
gem 'redis'

group :development do
  gem 'pry'
end

group :development, :test do
  gem 'rspec', '~> 3.0'
end
